apiVersion: v1
kind: Service
metadata:
  name: are-you-ready-service
  namespace: softctf-are-you-ready
spec:
  ports:
    - port: 80
      targetPort: 80
    - port: 443
      targetPort: 443
  selector:
    app: are-you-ready-app
  type: ClusterIP
